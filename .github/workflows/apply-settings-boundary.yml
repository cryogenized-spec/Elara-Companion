name: Apply Settings boundary migration (archived)

# One-shot migration retained only as an audit record.

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  archived:
    runs-on: ubuntu-latest
    steps:
      - run: echo 'Settings boundary migration is retained as an audit record only.'
