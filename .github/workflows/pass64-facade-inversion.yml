name: Pass 64 facade inversion (archived)

# Archived one-shot migration workflow. Deliberately disabled so historical surgery
# cannot execute again on future refactor-branch pushes.

on:
  workflow_dispatch:

jobs:
  archived:
    runs-on: ubuntu-latest
    steps:
      - run: echo 'Pass 64 migration is complete; this workflow is archived.'
