# Point 1 — Google provider extraction

Temporary trigger for the isolated Gmail extraction runner. The runner will perform the extraction, typecheck, commit the source cut, and remove its own workflow.
