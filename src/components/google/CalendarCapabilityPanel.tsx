import React, { useState } from 'react';
import { createCalendarEvent, getCalendarEventsRange } from '../../services/googleCalendarService';
import type { GoogleCalendarEvent } from '../../contracts';

export interface CalendarCapabilityPanelProps { canRead: boolean; canWrite: boolean; onActivity?: (description: string, reversible?: boolean) => void; onAskElara?: (prompt: string) => void; }

function busyEvents(events: GoogleCalendarEvent[]): { start: number; end: number }[] {
  return events.map((event) => {
    const start = event.start.dateTime ? Date.parse(event.start.dateTime) : event.start.date ? Date.parse(`${event.start.date}T00:00:00`) : NaN;
    const end = event.end.dateTime ? Date.parse(event.end.dateTime) : event.end.date ? Date.parse(`${event.end.date}T23:59:59`) : NaN;
    return { start, end };
  }).filter(({ start, end }) => Number.isFinite(start) && Number.isFinite(end) && end > start).sort((a,b)=>a.start-b.start);
}

function findOpenSlots(events: GoogleCalendarEvent[], from: Date, to: Date): { start: Date; end: Date }[] {
  const busy = busyEvents(events); const slots: { start: Date; end: Date }[] = []; let cursor = from.getTime();
  for (const item of busy) { const start = Math.max(item.start, from.getTime()); const end = Math.min(item.end, to.getTime()); if (start > cursor && start - cursor >= 30 * 60 * 1000) slots.push({start:new Date(cursor),end:new Date(start)}); cursor = Math.max(cursor, end); }
  if (to.getTime() - cursor >= 30 * 60 * 1000) slots.push({start:new Date(cursor),end:new Date(to)});
  return slots;
}

export function CalendarCapabilityPanel({ canRead, canWrite, onActivity, onAskElara }: CalendarCapabilityPanelProps) {
  const [events, setEvents] = useState<GoogleCalendarEvent[]>([]); const [range, setRange] = useState<'today'|'tomorrow'|'next7'>('today'); const [loading, setLoading] = useState(false); const [error, setError] = useState<string|null>(null); const [notice, setNotice] = useState<string|null>(null); const [title, setTitle] = useState(''); const [start, setStart] = useState(''); const [end, setEnd] = useState(''); const [location, setLocation] = useState(''); const [availability, setAvailability] = useState<{start:Date;end:Date}[]>([]);
  const load = async () => { if (!canRead) return; setLoading(true); setError(null); try { const now = new Date(); let startTime = new Date(now); let endTime = new Date(now); if (range === 'today') { startTime.setHours(0,0,0,0); endTime.setHours(23,59,59,999); } else if (range === 'tomorrow') { startTime.setDate(startTime.getDate()+1); startTime.setHours(0,0,0,0); endTime=new Date(startTime); endTime.setHours(23,59,59,999); } else { startTime.setHours(0,0,0,0); endTime.setDate(endTime.getDate()+7); } const result=await getCalendarEventsRange(startTime.toISOString(),endTime.toISOString(),50); setEvents(result.items as GoogleCalendarEvent[]); onActivity?.(`Read ${result.items.length} Calendar event${result.items.length===1?'':'s'}`); } catch(err){setError(err instanceof Error?err.message:'Unable to read Calendar.');} finally{setLoading(false);} };
  const create = async () => { if (!canWrite || !title.trim() || !start || !end) return; setError(null); try { const event = await createCalendarEvent(title.trim(), new Date(start).toISOString(), new Date(end).toISOString(), undefined, location.trim() || undefined); setEvents(prev=>[...prev,event as GoogleCalendarEvent]); setTitle(''); setStart(''); setEnd(''); setLocation(''); setNotice(`Created “${event.summary || title}”.`); onActivity?.(`Created Calendar event “${event.summary || title}”`, true); } catch(err){setError(err instanceof Error?err.message:'Unable to create Calendar event.');} };
  const findAvailability = async () => { if (!canRead) return; setLoading(true); setError(null); try { const from = new Date(); from.setHours(9,0,0,0); const to = new Date(from); to.setHours(17,0,0,0); const result = await getCalendarEventsRange(from.toISOString(), to.toISOString(), 50); const slots=findOpenSlots(result.items as GoogleCalendarEvent[],from,to); setAvailability(slots); onActivity?.(`Found ${slots.length} open Calendar slot${slots.length===1?'':'s'}`); } catch(err){setError(err instanceof Error?err.message:'Unable to find Calendar availability.');} finally{setLoading(false);} };
  return <div className="space-y-5"><div><div className="flex items-center justify-between gap-3"><div><p className="text-xs font-medium uppercase tracking-[0.16em] text-white/35">Scheduling</p><h3 className="mt-1 text-lg font-semibold">Google Calendar</h3></div>{onAskElara&&<button type="button" onClick={()=>onAskElara('Help me with my Google Calendar. Inspect my enabled schedule data and suggest useful next actions without making changes automatically.')} className="rounded-xl border border-sky-300/15 bg-sky-300/[0.04] px-3 py-2 text-xs text-sky-100">Ask Elara</button>}</div><p className="mt-1 text-sm text-white/50">Inspect today, tomorrow, or the next seven days, find open time, and create events.</p></div>{!canRead?<div className="rounded-2xl border border-amber-300/20 bg-amber-300/[0.05] p-4 text-sm text-amber-100">Calendar read access is not enabled. Enable the capability from Permissions.</div>:<><div className="flex flex-wrap gap-2">{(['today','tomorrow','next7'] as const).map(item=><button key={item} type="button" onClick={()=>setRange(item)} className={`rounded-lg border px-3 py-2 text-xs ${range===item?'border-white/20 bg-white/10':'border-white/10'}`}>{item==='today'?'Today':item==='tomorrow'?'Tomorrow':'Next 7 days'}</button>)}<button type="button" onClick={()=>void load()} disabled={loading} className="rounded-lg border border-white/10 px-3 py-2 text-xs">{loading?'Loading…':'Refresh'}</button><button type="button" onClick={()=>void findAvailability()} disabled={loading} className="rounded-lg border border-white/10 px-3 py-2 text-xs">Find availability</button><a href="https://calendar.google.com/" target="_blank" rel="noreferrer" className="rounded-lg border border-white/10 px-3 py-2 text-xs">Open Calendar</a></div>{availability.length>0&&<div className="rounded-2xl border border-emerald-300/15 bg-emerald-300/[0.04] p-4"><p className="text-xs font-semibold uppercase tracking-wide text-emerald-100/80">Open today · 09:00–17:00</p><div className="mt-2 flex flex-wrap gap-2">{availability.map(slot=><span key={slot.start.toISOString()} className="rounded-lg border border-emerald-300/10 bg-black/20 px-2.5 py-1.5 text-xs text-emerald-100">{slot.start.toLocaleTimeString([], {hour:'2-digit',minute:'2-digit'})}–{slot.end.toLocaleTimeString([], {hour:'2-digit',minute:'2-digit'})}</span>)}</div></div>}{error&&<div className="rounded-xl border border-red-400/20 bg-red-400/[0.06] px-4 py-3 text-sm text-red-200">{error}</div>}{notice&&<div className="rounded-xl border border-emerald-400/20 bg-emerald-400/[0.06] px-4 py-3 text-sm text-emerald-200">{notice}</div>}<section className="space-y-3 rounded-2xl border border-white/10 bg-white/[0.03] p-4"><h4 className="text-sm font-semibold">Create event</h4>{!canWrite&&<p className="text-xs text-amber-100/60">Calendar write access is not enabled. Enable it from Permissions before creating an event.</p>}<input disabled={!canWrite} value={title} onChange={e=>setTitle(e.target.value)} placeholder="Title" className="w-full rounded-xl border border-white/10 bg-black/20 px-3 py-2.5 text-sm disabled:opacity-40"/><div className="grid gap-2 sm:grid-cols-2"><input disabled={!canWrite} type="datetime-local" value={start} onChange={e=>setStart(e.target.value)} className="rounded-xl border border-white/10 bg-black/20 px-3 py-2.5 text-sm disabled:opacity-40"/><input disabled={!canWrite} type="datetime-local" value={end} onChange={e=>setEnd(e.target.value)} className="rounded-xl border border-white/10 bg-black/20 px-3 py-2.5 text-sm disabled:opacity-40"/></div><input disabled={!canWrite} value={location} onChange={e=>setLocation(e.target.value)} placeholder="Location (optional)" className="w-full rounded-xl border border-white/10 bg-black/20 px-3 py-2.5 text-sm disabled:opacity-40"/><button type="button" onClick={()=>void create()} disabled={!canWrite||!title.trim()||!start||!end} className="rounded-xl border border-white/10 px-4 py-2.5 text-sm font-medium disabled:opacity-40">{canWrite?'Create event':'Write access required'}</button></section><div className="divide-y divide-white/5 overflow-hidden rounded-2xl border border-white/10 bg-white/[0.03]">{events.length===0?<div className="px-4 py-8 text-center text-sm text-white/35">No events loaded yet.</div>:events.map(event=><div key={event.id} className="px-4 py-4"><p className="text-sm font-medium">{event.summary||'(Untitled event)'}</p><p className="mt-1 text-xs text-white/45">{event.start.dateTime||event.start.date||''}</p>{event.location&&<p className="mt-1 text-xs text-white/35">{event.location}</p>}</div>)}</div></>}</div>;
}
