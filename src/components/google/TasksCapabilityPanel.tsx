import React, { useMemo, useState } from 'react';
import { completeTask, createTask, getTasks, type TaskItem } from '../../services/googleTasksService';

export interface TasksCapabilityPanelProps { canUse: boolean; onActivity?: (description: string, reversible?: boolean) => void; onAskElara?: (prompt: string) => void; }

type TaskView = 'today' | 'upcoming' | 'all';

function isToday(value?: string): boolean {
  if (!value) return false;
  const date = new Date(value); const now = new Date();
  return date.getFullYear() === now.getFullYear() && date.getMonth() === now.getMonth() && date.getDate() === now.getDate();
}
function isFuture(value?: string): boolean { return !!value && new Date(value).getTime() > Date.now(); }

export function TasksCapabilityPanel({ canUse, onActivity, onAskElara }: TasksCapabilityPanelProps) {
  const [tasks, setTasks] = useState<TaskItem[]>([]); const [title, setTitle] = useState(''); const [view, setView] = useState<TaskView>('today'); const [loading, setLoading] = useState(false); const [error, setError] = useState<string | null>(null); const [notice, setNotice] = useState<string | null>(null);
  const load = async () => { if(!canUse)return; setLoading(true);setError(null);try{const result=await getTasks();setTasks(result.items);onActivity?.(`Read ${result.items.length} Google Task${result.items.length===1?'':'s}`);}catch(err){setError(err instanceof Error?err.message:'Unable to read Tasks.');}finally{setLoading(false);}};
  const add = async () => { if(!canUse||!title.trim())return;setError(null);try{const task=await createTask(title.trim());setTasks(prev=>[task,...prev]);setTitle('');setNotice('Task created.');onActivity?.(`Created Google Task “${task.title}”`,true);}catch(err){setError(err instanceof Error?err.message:'Unable to create task.');} };
  const complete = async (task:TaskItem) => { if(!canUse||task.status==='completed')return;setError(null);try{const updated=await completeTask(task.id);setTasks(prev=>prev.map(item=>item.id===task.id?{...item,...updated,status:'completed'}:item));setNotice(`Completed “${task.title}”.`);onActivity?.(`Completed Google Task “${task.title}”`,true);}catch(err){setError(err instanceof Error?err.message:'Unable to complete task.');} };
  const visible = useMemo(() => tasks.filter(task => view === 'all' || (view === 'today' ? isToday(task.due) : isFuture(task.due))), [tasks, view]);
  return <div className="space-y-5"><div><div className="flex items-center justify-between gap-3"><div><p className="text-xs font-medium uppercase tracking-[0.16em] text-white/35">Tasks</p><h3 className="mt-1 text-lg font-semibold">Google Tasks</h3></div>{onAskElara&&<button type="button" onClick={()=>onAskElara('Help me with today’s Google Tasks. Review the loaded tasks and suggest priorities before making changes.')} className="rounded-xl border border-sky-300/15 bg-sky-300/[0.04] px-3 py-2 text-xs text-sky-100">Ask Elara</button>}</div><p className="mt-1 text-sm text-white/50">Today’s tasks and upcoming work with lightweight creation and completion; Google Tasks remains the system of record.</p></div>{!canUse?<div className="rounded-2xl border border-amber-300/20 bg-amber-300/[0.05] p-4 text-sm text-amber-100">Task access is not enabled. Enable the capability from Permissions.</div>:<><div className="flex flex-wrap gap-2"><div className="flex min-w-0 flex-1 gap-1 rounded-xl border border-white/10 bg-black/20 p-1">{(['today','upcoming','all'] as const).map(item=><button key={item} type="button" onClick={()=>setView(item)} className={`rounded-lg px-3 py-2 text-xs ${view===item?'bg-white/10 text-white':'text-white/45'}`}>{item==='today'?'Today':item==='upcoming'?'Upcoming':'All'}</button>)}</div><button type="button" onClick={()=>void load()} disabled={loading} className="rounded-xl border border-white/10 px-4 py-2.5 text-sm">{loading?'Loading…':'Refresh tasks'}</button><a href="https://tasks.google.com/" target="_blank" rel="noreferrer" className="rounded-xl border border-white/10 px-4 py-2.5 text-sm">Open Google Tasks</a></div><div className="flex gap-2"><input value={title} onChange={e=>setTitle(e.target.value)} onKeyDown={e=>{if(e.key==='Enter')void add();}} placeholder="New task…" className="min-w-0 flex-1 rounded-xl border border-white/10 bg-black/20 px-3 py-2.5 text-sm"/><button type="button" onClick={()=>void add()} disabled={!title.trim()} className="rounded-xl border border-white/10 px-4 py-2.5 text-sm disabled:opacity-40">Create</button></div>{error&&<div className="rounded-xl border border-red-400/20 bg-red-400/[0.06] px-4 py-3 text-sm text-red-200">{error}</div>}{notice&&<div className="rounded-xl border border-emerald-400/20 bg-emerald-400/[0.06] px-4 py-2.5 text-sm text-emerald-200">{notice}</div>}<div className="divide-y divide-white/5 overflow-hidden rounded-2xl border border-white/10 bg-white/[0.03]">{visible.length===0?<div className="px-4 py-8 text-center text-sm text-white/35">No tasks in this view yet.</div>:visible.map(task=><div key={task.id} className="flex items-center gap-3 px-4 py-3"><span className={`h-2.5 w-2.5 shrink-0 rounded-full ${task.status==='completed'?'bg-emerald-300':'bg-white/30'}`}/><div className="min-w-0 flex-1"><p className={`text-sm ${task.status==='completed'?'text-white/45 line-through':'text-white/80'}`}>{task.title}</p>{task.due&&<p className="mt-1 text-xs text-white/35">Due {new Date(task.due).toLocaleString()}</p>}</div>{task.status!=='completed'&&<button type="button" onClick={()=>void complete(task)} className="rounded-lg border border-white/10 px-3 py-2 text-xs font-medium">Complete</button>}</div>)}</div></>}</div>;
}
