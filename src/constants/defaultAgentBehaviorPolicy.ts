/**
 * Backward-compatible export for older V3 imports.
 * The canonical policy now lives in src/lib/agentPolicy.ts.
 */
export { DEFAULT_AGENT_OPERATING_POLICY as DEFAULT_AGENT_BEHAVIOR_POLICY } from '../lib/agentPolicy';
