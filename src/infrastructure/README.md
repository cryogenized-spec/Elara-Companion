# Infrastructure Boundary

Platform and external-system adapters belong here.

Planned responsibilities include IndexedDB/local persistence, browser APIs, HTTP/SSE transport, authentication transport, and provider-specific adapters.

Infrastructure should expose explicit interfaces upward rather than leaking implementation details into React components.
