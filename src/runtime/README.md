# Runtime Boundary

Execution lifecycle code belongs here.

Planned responsibilities include Gemini/model execution, streaming, retries, cancellation, tool loops, background execution, and runtime resilience.

The runtime boundary should expose application-level operations rather than requiring UI code to understand transport or provider mechanics.
